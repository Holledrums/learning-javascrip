const myNumbers = [2, 5, 3, 6, 8, 9, 7];
const result = myNumbers.map((element) => element * 3);
console.log(result);
// das ausgegebene Array ist nur eine Kopie des bestehenden Array mit veränderten werten.
